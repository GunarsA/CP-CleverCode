#include<iostream>
using namespace std;

int izmers, skaits;

int main(){
    cin>>izmers;
    cin>>skaits;
    bool laukums[izmers][izmers]={false};
    int pentam[skaits][2];
    char penTam[skaits]
    for(rnd1=0;rnd1<skaits;rnd1++){
        cin<<penTam[rnd1]<<pentam[rnd1][0]<<pentam[rnd1][1];
    }
    for(rnd2=0;rnd2<skaits;rnd2++){
        switch(penTam[rdn2]){
            case'A':laukums[pentam[rnd2][0]][pentam[rnd2][1]]=true;                   
                    laukums[(pentam[rnd2][0])][pentam[rnd2][1]]=true;
                    laukums[pentam[rnd2][0]][pentam[rnd2][1]]=true;
                    laukums[pentam[rnd2][0]][pentam[rnd2][1]]=true;
                    laukums[pentam[rnd2][0]][pentam[rnd2][1]]=true;
            case 'B' :laukums[pentam[rnd2][0]][pentam[rnd2][1]]=true;
            case 'C' :laukums[pentam[rnd2][0]][pentam[rnd2][1]]=true;
            case 'D' :laukums[pentam[rnd2][0]][pentam[rnd2][1]]=true;
            case 'E' :laukums[pentam[rnd2][0]][pentam[rnd2][1]]=true;
        }
    }



    return 0;
}